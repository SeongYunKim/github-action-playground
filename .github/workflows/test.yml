name: Test

on:
  push:
    branches:
      - 'test/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: |
          echo ${{GITHUB_SHA}}_${{GITHUB_REF_NAME}}
          echo $GITHUB_REF
          echo $GITHUB_REF_NAME
